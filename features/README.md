# Features

This directory describes current implemented product behaviour.

Feature files are concise behavioural references for understanding the system as
it exists today and for checking regressions.

Use:

- `features/*.md` for current behaviour
- `plans/*.md` for future implementation work
- `archive/plans/*.md` for historical implementation steps
